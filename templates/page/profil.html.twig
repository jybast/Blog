{% extends 'base.html.twig' %}

{% block title %}Informations utilisateur
{% endblock %}

{% block body %}
	<style>
		.example-wrapper {
			margin: 1em auto;
			max-width: 800px;
			width: 95%;
			font: 18px / 1.5 sans-serif;
		}
		.example-wrapper code {
			background: #F5F5F5;
			padding: 2px 6px;
		}
	</style>
	<div class="row">
		<div class="col-4">
            <div class="banniere-logo">
                <img src="{{ asset('/ressources/img/cc_transparent.png') | imagine_filter('miniature') }}" alt="logo">
            </div>
        </div>
		<div class="col-8">
			<div class="example-wrapper">
				<h1>Bonjour
					{{ app.user.nom }}! ✅</h1>

				{% for message in app.flashes('success') %}
					<div class="alert alert-success" role="alert">{{ message }}</div>
				{% endfor %}

				Page du profil utilisateur:
				<ul>
					<li>Your controller at
						<code>
							<a href="{{ 'D:/wamp64/www/Blog/src/Controller/PageController.php'|file_link(0) }}">src/Controller/PageController.php</a>
						</code>
					</li>
					<li>Your template at
						<code>
							<a href="{{ 'D:/wamp64/www/Blog/templates/page/profil.html.twig'|file_link(0) }}">templates/page/profil.html.twig</a>
						</code>
					</li>
				</ul>
			</div>

		</div>

	{% endblock %}
